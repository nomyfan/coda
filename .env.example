# Provider API keys, referenced from coda-server.toml as ${VAR}.
# base_url and model now live in coda-server.toml.
DEEPSEEK_API_KEY=
OPENAI_API_KEY=

# PostgreSQL connection string, referenced from coda-server.toml as ${DATABASE_URL}.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/coda
